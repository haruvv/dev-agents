name: test-label-trigger

on:
  issues:
    types: [labeled]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "triggered by label ${{ github.event.label.name }} on issue ${{ github.event.issue.number }}"
